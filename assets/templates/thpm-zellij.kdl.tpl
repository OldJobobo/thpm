themes {
    thpm-current {
        fg "{{ fg }}"
        bg "{{ bg }}"
        black "{{ darker_bg }}"
        red "{{ red }}"
        green "{{ green }}"
        yellow "{{ yellow }}"
        blue "{{ blue }}"
        magenta "{{ magenta }}"
        cyan "{{ cyan }}"
        white "{{ bright_fg }}"
        orange "{{ orange }}"
    }
}
