# fzf certification — 2026-08-28

## Identity

- Integration ID: `fzf`
- Live-tested adapter source commit: `2713c314a223a1ce3d0d4d572ed395f626c3b9c5`
- Certification branch: `agent/validate-fzf`; its candidate changes add tests, lifecycle metadata, and retained evidence without changing the live-tested fzf adapter/template.
- Development module used by the installed hook: `/tmp/thpm-fzf-dev/src/thpm/__init__.py`
- Installed command retained for contrast: `/usr/bin/thpm`
- Omarchy: `4.0.1-1`
- fzf: `0.74.3 (15f64c49)` / package `0.74.3-1`
- Fish: `4.8.1-1`
- Foot: `1.27.0-2`
- Test host: disposable Omarchy Quattro VM

## Fixtures and user lifecycle

Both transitions used Omarchy's normal top-level command with the development THPM executable first on the hook `PATH`:

```text
omarchy-theme-set "Dune"
omarchy-theme-set "Last Call"
```

Palette fixture paths and hashes:

- Dune: `~/.config/omarchy/themes/dune/colors.toml` — `aa95316a9b95b7855512e6b9da30de03a4ca1ff3d384cd0dd1c62def87d878c3`
- Last Call: `~/.config/omarchy/themes/last-call/colors.toml` — `2fa96820d96b7cac89a90b4f4726ca019a27608250e83d3a370845496ad856cb`

The exact application launch shape was:

```text
systemd-run --user --unit=thpm-fzf-evidence --collect \
  /usr/bin/foot --app-id=thpm-fzf-evidence --title="THPM fzf — <theme>" \
  --window-size-chars=100x32 /usr/bin/fish -lc \
  'printf "%s\n" <semantic-fixture-lines> | fzf --multi --layout=reverse \
  --border=rounded --padding=1 --header="THPM · omarchy-theme-set <theme> · fzf 0.74.3" \
  --prompt="Search <theme> › " --pointer="▶" --marker="✓" --info=inline-right \
  --query=<theme-query>'
```

The [complete literal fixture commands](evidence/fzf-2026-08-28/commands.txt) and both resulting frames are retained in the evidence bundle. Fish loaded `~/.config/fish/conf.d/thpm-fzf.fish`; no `FZF_DEFAULT_OPTS` value was injected directly into the launch command. The fzf view exposed normal and selected rows, query highlights, header, prompt, pointer, marker, info count, border, foreground, and background.

## Results

- Initial state: integration disabled; generated current-theme output and Fish destination absent.
- Dune transition: `fzf applied`; the destination contained the Dune semantic palette.
- Same-theme no-op: `fzf unchanged`; destination inode, nanosecond mtime, size, and SHA-256 remained stable. Dune destination SHA-256: `10b78514811ab8fbea675bee3c7bedb775719901e52d122ac6c100f942168fa1`.
- Last Call transition: `fzf applied`; a newly launched Fish/fzf process visibly changed from Dune brown/gold to Last Call teal/cyan/rust/green.
- Reload contract: no running-process restart was claimed. A new Fish process consumed the changed `conf.d` environment, matching Fish's real lifecycle.
- Disable/restoration: disabling an unchanged managed destination removed the destination, current-theme output, and template, restoring the absent baseline. A newly launched Fish process then reported an empty `FZF_DEFAULT_OPTS`; a fresh fzf process completed the application-level restoration check with no THPM `--color` option or stale semantic mapping.
- User-edit protection: after a synthetic recognizable edit, disable preserved the file and emitted `preserved user-modified file instead of restoring it`.
- Uninstall/restoration: uninstall removed the managed destination and THPM product state. Before reinstall, a newly launched Fish process reported an empty `FZF_DEFAULT_OPTS`, and a fresh fzf process visibly used its default palette with no THPM `--color` option. Reinstall succeeded; the exact pre-test state file was restored and matched byte-for-byte, `fzf = false`, and the fzf destination remained absent.
- Redacted support report: [`evidence/fzf-2026-08-28/thpm-fzf-certification-report.json`](evidence/fzf-2026-08-28/thpm-fzf-certification-report.json), SHA-256 `bd7e81b11fc9b06319abaeaa0fed08c1e0640375ed67d1659a327c1690ed6612`. A final enable/report/disable cycle again restored the original state byte-for-byte.

## Visual evidence

- [Dune screenshot](evidence/fzf-2026-08-28/thpm-fzf-dune-marked.png)
  - SHA-256: `01ad616802cd912a866805abe6366e388e61499f67ca0ce3af5d2d0662b2072c`
- [Last Call screenshot](evidence/fzf-2026-08-28/thpm-fzf-last-call-marked.png)
  - SHA-256: `bca5e6567cf1ef7fd7c57f0f0929fe930d319d7bce5eef8993eac803cdca4aae`
- [Fresh fzf after disable](evidence/fzf-2026-08-28/thpm-fzf-post-disable-fresh.png)
  - SHA-256: `636b2a98c366f0249b32d3123378215ba17c816848badfee25f85e765a7341e3`
- [Fresh fzf after uninstall](evidence/fzf-2026-08-28/thpm-fzf-post-uninstall-fresh.png)
  - SHA-256: `a8402e8f049209fbbc9f64d039c3c44459a88452042c5a3e00ca7acbe2a9d4d6`
- [Fresh Fish environment after disable](evidence/fzf-2026-08-28/post-disable-env.txt)
  - SHA-256: `7d59b8c9e994c3c4ef516f6f35941b846e714cd40159f7b1e4bccc63ac0c18a1`
- [Fresh Fish environment after uninstall](evidence/fzf-2026-08-28/post-uninstall-env.txt)
  - SHA-256: `7d59b8c9e994c3c4ef516f6f35941b846e714cd40159f7b1e4bccc63ac0c18a1`

The Dune and Last Call frames show the complete themed fzf surface inside Foot. The post-disable and post-uninstall frames are separate fresh Fish/fzf processes whose launch commands fail closed if THPM `--color` options remain; they visibly show fzf's restored default palette. The paired environment logs retain the exact empty `FZF_DEFAULT_OPTS` observations. Copies were also delivered in the THPM audit's Discord thread.

## Signoff

All technical, automated, lifecycle, restoration, and visual gates passed on 2026-08-28.

- Maintainer: **OldJobobo**
- Signoff date: **2026-08-28**
- Decision: **Approved promotion of fzf to Supported**, while retaining its opt-in and default-disabled policy.
