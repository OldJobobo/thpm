# Zellij certification — 2026-08-28

## Identity

- Integration ID: `zellij`
- Exact merged main revalidated: `a08526e43b7b988617873625c45edebea2d3071d`
- Zellij lifecycle-fix merge: `246c3fad459fd815d5bbdf05b5e6606bf1f0968c`
- Exact-source VM checkout: `/tmp/thpm-cert-a08526e`
- Omarchy: `4.0.1-1`
- Zellij: `0.45.0`
- Test host: disposable Omarchy Quattro VM
- Original VM theme: `Last Call`

The current merged Zellij adapter function block and the block used for the live screenshots have the same SHA-256: `4d9c6e6db07c348528bca2825abd5b4f5e45900ef63f6c29204c2a8dced0c01f`. Current `service.py` and `integrations.py` were copied from canonical main and hash-matched on the VM before the merged-source lifecycle rerun.

## Fixtures and real lifecycle

The real desktop lifecycle used Omarchy's normal top-level command with the development THPM wrapper first on the graphical hook `PATH`:

```text
omarchy-theme-set "Dune"
omarchy-theme-set "Last Call"
```

Fixture hashes:

- Dune `colors.toml`: `aa95316a9b95b7855512e6b9da30de03a4ca1ff3d384cd0dd1c62def87d878c3`
- Last Call `colors.toml`: `2fa96820d96b7cac89a90b4f4726ca019a27608250e83d3a370845496ad856cb`

The same named `thpm-zellij-evidence` session remained running across the transition. Zellij's pane frames, selected floating pane, tab/ribbon, mode indicator, status/help bar, and terminal text visibly changed from Dune's brown/gold palette to Last Call's dark teal/cyan/slate palette without restarting the session.

## Results

- **Initial effect:** development-source provenance was recorded before the normal Dune lifecycle event. Zellij selected `thpm-current`, parsed the installed KDL, and visibly rendered the complete Dune palette.
- **No-op:** a second ordinary Dune event reported unchanged; `config.kdl` and `themes/thpm.kdl` retained identical inode, timestamp, size, and SHA-256 metadata (`same_theme_noop_metadata_stable=true`).
- **Theme transition:** normal `omarchy-theme-set "Last Call"` changed the same still-running Zellij 0.45.0 session across all observed Zellij-owned surfaces.
- **Disable/restoration:** cleanup restored the prior theme selection and any file displaced at `themes/thpm.kdl`. The exact merged harness matched both restored files to their expected baselines byte-for-byte.
- **User modification:** in an exact merged-source sidecar, a recognizable edit to managed `themes/thpm.kdl` was preserved on disable; THPM emitted the preservation warning, restored `theme "default"`, removed the `thpm-current` selection, and Zellij's own parser accepted the before/after configurations.
- **Uninstall/restoration:** exact merged uninstall returned `ok: true` and `cleanupIncomplete: false`, with no residuals, errors, or warnings. The prior config and displaced theme matched their baseline hashes byte-for-byte, and Zellij reported `[CONFIG FILE]: Well defined.` before application and after uninstall.
- **Restart reporting:** the merged lifecycle implementation detects exact-name, same-user Zellij processes and reports a restart only when disable or uninstall changes managed Zellij output. Focused regressions cover changed and no-op disable/uninstall, cross-user processes, and disappearing `/proc` entries.
- **Original state:** the disposable VM returned to `Last Call`; its original absent Zellij config/theme baseline and absence of running evidence sessions were restored.

The [complete command and output record](evidence/zellij-2026-08-28/commands.txt) retains source hashes, lifecycle commands, parser results, restoration hashes, and exact merged-source revalidation.

## Visual evidence

- [Dune screenshot](evidence/zellij-2026-08-28/zellij-dune.png)
  - SHA-256: `11eeb389e7f966493f9c972ab27d2f77013120836750ff19e20653898240068c`
- [Last Call screenshot](evidence/zellij-2026-08-28/zellij-last-call.png)
  - SHA-256: `922b0308f2b0b0d9fa5e4a059d8d9fe2a1421ef659c89b0e637303b45febd9ab`

The Dune frame shows coherent brown/gold colors across the outer frame, selected floating-pane border, ribbons, mode/status labels, help bar, and text. The Last Call frame shows a distinct dark teal/cyan/slate treatment across those same surfaces in the same session. No white/default fallback frame or stale Dune-owned chrome remains in the Last Call evidence.

## Signoff

All required artifact, parser, real-application, no-op, live-transition, disable, user-modification, uninstall, restoration, and restart-reporting gates passed on 2026-08-28.

- Maintainer: **OldJobobo**
- Signoff date: **2026-08-28**
- Decision: **Approved promotion of Zellij to Supported**, while retaining its opt-in and default-disabled policy.
